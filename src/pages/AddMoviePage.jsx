import React from "react";
import MovieAddForm from "../component/uiComponents/MovieAddForm";

const AddMoviePage = () => {
  return (
    <section className="h-screen w-screen flex items-center justify-center bg-slate-700">
      <MovieAddForm />
    </section>
  );
};

export default AddMoviePage;
